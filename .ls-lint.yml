ls:
  .dir: regex:([.a-z0-9]*)([-.][a-z0-9]+)*
  .ts: regex:([.a-z0-9]*)([-.][a-z0-9]+)*
  .tsx: regex:([.a-z0-9]*)([-.][a-z0-9]+)*

ignore:
  - .git
  - node_modules
  - gses-app/node_modules
  - gses-app/build
  - logger-app/node_modules
  - logger-app/build